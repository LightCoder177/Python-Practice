#Checking for vowels with their repeated counts in a sentence without showing the non-repeated ones.

#list, dict, and variable.

vowels = ['a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U']
found = {}

word = input("Type your sentence here: ")

#loop, condition, and execution. 

for letter in word:
    if letter in vowels:
        found.setdefault(letter, 0)
        found[letter] += 1

print("Here are the vowels with their repeated counts in the sentence that you provided.")

for vowel, count in found.items():
    print("{0:s} found {1:d} time(s).".format(vowel, count))
